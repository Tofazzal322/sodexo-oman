import React, { useEffect } from 'react';

const useData = () => {
    useEffect(() => {
  fetch('')
  .then(res=>)
},[])
    return (
        <div>
            
        </div>
    );
};

export default useData;